---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: radarr-secret-push
  namespace: media
spec:
  refreshInterval: 1h
  deletionPolicy: None
  secretStoreRefs:
    - name: openbao
      kind: ClusterSecretStore
  selector:
    secret:
      name: radarr-secret
  data:
    - match:
        secretKey: RADARR__AUTH__APIKEY
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__HOST
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__PORT
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__USER
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__PASSWORD
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__MAINDB
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: RADARR__POSTGRES__LOGDB
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: INIT_POSTGRES_HOST
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: INIT_POSTGRES_USER
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: INIT_POSTGRES_PASS
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: INIT_POSTGRES_DBNAME
        remoteRef:
          remoteKey: media/radarr-secret
    - match:
        secretKey: INIT_POSTGRES_SUPER_PASS
        remoteRef:
          remoteKey: media/radarr-secret
