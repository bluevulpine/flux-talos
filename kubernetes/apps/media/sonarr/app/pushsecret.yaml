---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/pushsecret_v1alpha1.json
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: sonarr-secret-push
  namespace: media
spec:
  refreshInterval: 1h
  deletionPolicy: None
  secretStoreRefs:
    - name: openbao
      kind: ClusterSecretStore
  selector:
    secret:
      name: sonarr-secret
  data:
    - match:
        secretKey: SONARR__AUTH__APIKEY
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__HOST
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__PORT
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__USER
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__PASSWORD
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__MAINDB
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: SONARR__POSTGRES__LOGDB
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: INIT_POSTGRES_HOST
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: INIT_POSTGRES_USER
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: INIT_POSTGRES_PASS
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: INIT_POSTGRES_DBNAME
        remoteRef:
          remoteKey: media/sonarr-secret
    - match:
        secretKey: INIT_POSTGRES_SUPER_PASS
        remoteRef:
          remoteKey: media/sonarr-secret
