---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app rreading-glasses
spec:
  chartRef:
    kind: OCIRepository
    name: rreading-glasses
  interval: 1h
  values:
    controllers:
      rreading-glasses:
        type: deployment
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          01-init-db:
            image:
              repository: ghcr.io/home-operations/postgres-init
              tag: rolling
            envFrom:
              - secretRef:
                  name: rreading-glasses-secret
        containers:
          app:
            image:
              repository: docker.io/blampe/rreading-glasses
              # Use 'hardcover' tag for Hardcover metadata (higher quality, requires fresh Readarr DB)
              # Use 'latest' tag for GoodReads metadata (backward compatible)
              tag: hardcover
            command:
              - /main
              - serve
              - --verbose
            env:
              # Hardcover auth token passed via secret
              # For GoodReads, use: --upstream=www.goodreads.com instead
            envFrom:
              - secretRef:
                  name: rreading-glasses-secret
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: &port 8788
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 3
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: {drop: ["ALL"]}
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 256Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 568
        runAsGroup: 568
        fsGroup: 568
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        controller: rreading-glasses
        ports:
          http:
            port: *port

