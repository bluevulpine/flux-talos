---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ./authorized-keys.yaml
  - ./init-configmap.yaml
  - ./statefulset.yaml
  - ./service.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/name: dev-shell
      app.kubernetes.io/part-of: dev-shell
