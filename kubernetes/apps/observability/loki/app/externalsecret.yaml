---
# Secret for Loki SeaweedFS S3 credentials
apiVersion: v1
kind: Secret
metadata:
  name: loki-bucket
type: Opaque
data:
  # SeaweedFS default credentials (base64 encoded)
  # For now using default credentials since SeaweedFS auth is disabled
  AWS_ACCESS_KEY_ID: YWRtaW4=  # admin
  AWS_SECRET_ACCESS_KEY: YWRtaW4=  # admin
