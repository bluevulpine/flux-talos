---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: observability
components:
  - ../../components/common
  - ../../components/psa-privileged

resources:
  - ./namespace.yaml
  # - ./alertmanager-silencer/ks.yaml
  - ./fluent-bit/ks.yaml
  - ./gatus/ks.yaml
  - ./grafana/ks.yaml
  - ./kromgo/ks.yaml
  - ./kube-prometheus-stack/ks.yaml
  # - ./loki/ks.yaml  # Removed - replaced by victoria-logs + fluent-bit
  - ./smartctl-exporter/ks.yaml
  # - ./snmp-exporter/ks.yaml
  - ./thanos/ks.yaml
  - ./unpoller/ks.yaml
  # - ./vector/ks.yaml  # Removed - was sending to Loki which is disabled; fluent-bit handles log collection
  - ./victoria-logs/ks.yaml
