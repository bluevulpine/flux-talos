---
# ExternalSecret for Thanos Minio credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: thanos-bucket
spec:
  secretStoreRef:
    name: bitwarden-secrets-manager
    kind: ClusterSecretStore
  target:
    name: thanos-bucket
    template:
      engineVersion: v2
      data:
        AWS_ACCESS_KEY_ID: "{{ .Minio__RootUser }}"
        AWS_SECRET_ACCESS_KEY: "{{ .Minio__RootPass }}"
  dataFrom:
    - extract:
        key: minio
