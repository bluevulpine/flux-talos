---
# ConfigMap for Thanos Garage S3 bucket configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: thanos-bucket
data:
  BUCKET_NAME: "thanos"
  # BUCKET_HOST now comes from ExternalSecret (Garage__Host)
  BUCKET_REGION: "us-east-1"
