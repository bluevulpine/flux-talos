---
# ServiceAccount the CronJobs authenticate to OpenBao with, via the Kubernetes
# auth method (OpenBao role: kia-collector, bound to this SA in namespace home).
# Replaces the former static OPENBAO_TOKEN — no long-lived token to rotate.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kia-collector
