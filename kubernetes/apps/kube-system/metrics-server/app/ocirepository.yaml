---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/source.toolkit.fluxcd.io/ocirepository_v1.json
apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: metrics-server
spec:
  interval: 15m
  layerSelector:
    mediaType: application/vnd.cncf.helm.chart.content.v1.tar+gzip
    operation: copy
  ref:
    tag: 3.13.1
  url: oci://ghcr.io/home-operations/charts-mirror/metrics-server
  # verify block removed 2026-06-22 — home-operations/charts-mirror stopped publishing keyless cosign
  # signatures (confirmed: "no signatures found", not a network error). Re-enable once upstream
  # signing is restored and tested: https://github.com/home-operations/charts-mirror

