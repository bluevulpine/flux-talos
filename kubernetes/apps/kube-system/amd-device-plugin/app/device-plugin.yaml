---
# AMD GPU Device Plugin DaemonSet
# Exposes AMD GPUs as schedulable resources (amd.com/gpu)
# Source: https://github.com/ROCm/k8s-device-plugin
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: amdgpu-device-plugin
  labels:
    app.kubernetes.io/name: amd-device-plugin
    app.kubernetes.io/component: device-plugin
spec:
  selector:
    matchLabels:
      name: amdgpu-device-plugin
  template:
    metadata:
      labels:
        name: amdgpu-device-plugin
        app.kubernetes.io/name: amd-device-plugin
        app.kubernetes.io/component: device-plugin
    spec:
      restartPolicy: Always
      nodeSelector:
        kubernetes.io/arch: amd64
        # Only deploy on nodes with AMD GPUs (detected by NFD)
        feature.node.kubernetes.io/pci-0300_1002.present: "true"
      priorityClassName: system-node-critical
      tolerations:
        - key: CriticalAddonsOnly
          operator: Exists
      containers:
        - name: device-plugin
          # renovate: datasource=docker depName=rocm/k8s-device-plugin
          image: docker.io/rocm/k8s-device-plugin:1.31.0.10@sha256:0555caf9ccc1cf407b353d1aade87d4598059f87a784085aafe3ece19405b612
          imagePullPolicy: IfNotPresent
          securityContext:
            privileged: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 10m
              memory: 20Mi
            limits:
              cpu: 100m
              memory: 50Mi
          volumeMounts:
            - name: device-plugins
              mountPath: /var/lib/kubelet/device-plugins
            - name: sys
              mountPath: /sys
              readOnly: true
      volumes:
        - name: device-plugins
          hostPath:
            path: /var/lib/kubelet/device-plugins
        - name: sys
          hostPath:
            path: /sys

