---
# (d) forward direction: backup from smoke-dst. Identical to 01-src.yaml RS except sourcePVC/namespace. smoke-dst has NO privileged-movers annotation.
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata: {name: smoke-local, namespace: smoke-dst, labels: {smoke-test: hermes-ns-move}}
spec:
  sourcePVC: smokedata-c
  trigger: {manual: run-1}
  kopia:
    copyMethod: Snapshot
    repository: smoke-volsync-local-secret
    username: smoketest
    volumeSnapshotClassName: longhorn-snapclass
    cacheCapacity: 2Gi
    cacheStorageClassName: longhorn-1-replica
    cacheAccessModes:
      - ReadWriteOnce
    storageClassName: longhorn-1-replica
    accessModes:
      - ReadWriteOnce
    capacity: 1Gi
    moverSecurityContext: {runAsUser: 0, runAsGroup: 0, fsGroup: 0, fsGroupChangePolicy: OnRootMismatch}
    retain: {latest: 2}
